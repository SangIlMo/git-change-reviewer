diff --git a/src/helpers/formatDate.ts b/src/utils/date.ts
similarity index 52%
rename from src/helpers/formatDate.ts
rename to src/utils/date.ts
index 3a4b5c6..7d8e9f0 100644
--- a/src/helpers/formatDate.ts
+++ b/src/utils/date.ts
@@ -1,10 +1,18 @@
-export function formatDate(date: Date): string {
-  return date.toISOString().split('T')[0];
-}
+import { format, parseISO } from 'date-fns';

-export function formatDateTime(date: Date): string {
-  return date.toISOString();
+export function formatDate(date: Date | string, pattern = 'yyyy-MM-dd'): string {
+  const d = typeof date === 'string' ? parseISO(date) : date;
+  return format(d, pattern);
+}
+
+export function formatDateTime(date: Date | string): string {
+  const d = typeof date === 'string' ? parseISO(date) : date;
+  return format(d, "yyyy-MM-dd'T'HH:mm:ssxxx");
+}
+
+export function isExpired(date: Date | string, nowMs = Date.now()): boolean {
+  const d = typeof date === 'string' ? parseISO(date) : date;
+  return d.getTime() < nowMs;
 }
